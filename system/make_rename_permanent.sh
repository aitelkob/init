# Linux
# Edit /etc/hostname for the hostname change to persist.
# Additionally, update /etc/hosts to reflect the new hostname.

# macOS
# The change made with `scutil --set HostName` is already permanent.

