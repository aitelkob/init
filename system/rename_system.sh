# Linux
hostnamectl set-hostname new-name # Sets the system's hostname to 'new-name'.
# Permanent change requires editing /etc/hostname and possibly /etc/hosts.

# macOS
sudo scutil --set HostName new-name # Sets the system's hostname to 'new-name'.
# This change is immediate and persists across reboots.

