# Both Linux and macOS
# Linux uses cron, accessed via 'crontab -e'.
# macOS also supports cron, though launchd is preferred, configured with .plist files in /Library/LaunchAgents/ or ~/Library/LaunchAgents/.

