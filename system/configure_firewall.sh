# Linux (Using UFW as an example)
# sudo ufw allow from 10.0.0.0/8 to any port [port_number]
# sudo ufw deny all

# macOS
# PF firewall configurations involve editing /etc/pf.conf directly or using pfctl commands.
# Example: Block all except 10.0.0.0/8
# (Commands to modify /etc/pf.conf or use pfctl to implement similar rules.)

